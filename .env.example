# Supabase "Session pooler" URL (IPv4, port 5432). URL-encode special characters in the password.
DATABASE_URL=postgresql://postgres.project-ref:password@aws-0-region.pooler.supabase.com:5432/postgres
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
OPENAI_API_KEY=
OPENAI_MODEL=
LLM_ENABLED=false
VISA_SPONSORSHIP_REQUIRED=false
COMPANIES_CONFIG=config/companies.yml
PROFILES_CONFIG=config/profiles.yml
PREFERENCES_CONFIG=config/preferences.yml
# For private candidate eligibility/commuting facts, use config/preferences.local.yml.
# See docs/candidate-eligibility.md; never commit that private file.
MONITOR_TIMEZONE=America/New_York
MONITOR_HOUR=20
SCHEDULE_GRACE_HOURS=16
IMMEDIATE_NOTIFICATION_MIN_SCORE=0.82
IMMEDIATE_NOTIFICATION_MAX_SOURCE_AGE_DAYS=21
IMMEDIATE_NOTIFICATION_MAX_PER_RUN=5
DAILY_SUMMARY_MAX_MATCHES=15
DAILY_SUMMARY_MAX_REVIEWS=5
# Optional: set to a local text/Markdown resume, for example config/resume.md
RESUME_PATH=
